//an=(2*n^3+1)/(5*n^3+1)
#include <stdio.h>
#include <math.h>
int main()
{
    int n,i=0;
    double an,n3;
    printf("input n:\n");
    scanf("%d",&n);
    while (i<=n)
    {
        n3=pow(i,3);
        an=(2*n3+1)/(5*n3+1);       
        printf("a%d=%lf\n",i,an);
		 i++;
    }
    return 0;
}
